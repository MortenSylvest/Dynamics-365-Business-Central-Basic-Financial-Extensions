The four files are work files and probably contains some sort of errors.
- 365BC_PagesAsText.txt (Export from D365BC)
- FindApplicationArea.ps1 (Quick and dirty mockup, to identify Application Area in PagesAsText)
- 365BC_PagesAsText.csv (Output file from FindApplicationArea.ps1)
- NewApplicationArea.xlsx (Manually select which ones to have a New Application Area)
